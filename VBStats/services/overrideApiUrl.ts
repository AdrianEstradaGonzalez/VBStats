// Optional override for API URL during development.
// Create this file in your workspace (not committed to VCS) to force the app
// to use a remote backend while running in the emulator.

// Example:
// export const API_OVERRIDE_URL = 'https://vbstats.onrender.com/api';

export const API_OVERRIDE_URL = 'https://vbstats.onrender.com/api';
